// Re-export from shared utility for backward compatibility
export {
  formatDate,
  formatWeekday,
  formatDateHeader,
  formatMonth,
  formatMonthYear,
  toDateKey,
  getDateNumber,
  formatDateWithDay,
  formatDateRange,
} from '@/shared/lib/dateUtils';
