// 메인 컴포넌트 (주 사용)
export { Calendar } from './Calendar';

// 타입 (필요 시 외부 사용)
export type {
  CalendarProps,
  CalendarView,
  DateCellProps,
  IndicatorProps,
  ViewSwitcherProps,
  WeekViewProps,
  MonthViewProps,
} from './types';
