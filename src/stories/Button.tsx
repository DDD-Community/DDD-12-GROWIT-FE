const Button = () => {
  return (
    <button className="border border-gray-300 rounded-lg bg-blue-500 px-4 py-3 rounded-lg text-white font-semibold cursor-pointer">
      버튼
    </button>
  );
};

export default Button;
